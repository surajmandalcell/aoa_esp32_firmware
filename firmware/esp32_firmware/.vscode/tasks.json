{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "arduino build",
      "type": "shell",
      "command": "arduino-cli compile --fqbn arduino:esp32:nano_nora --build-path ./.build ."
    }
  ]
}
